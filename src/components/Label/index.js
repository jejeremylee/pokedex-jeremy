import React from "react";
import {LabelContainer} from "./style.js";

export default function Label(props){
       return(
        <LabelContainer>
            type
       </LabelContainer>
       );
}